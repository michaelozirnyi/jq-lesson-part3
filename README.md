# jq-lesson-part3

##command for install bower
```  bower install ```

##install sass
``` gem install sass ```

###install watch
``` gem install compass ```

####compile scss command
``` compass watch ``